#include<stdio.h>

int binario (int decimal)
{
    int aux;
    aux = decimal;
    while (aux >= 0)
    {
        
        aux /= 2;

    }
    
}
int main()
{
    /*este programa transforma um numero decimal inteiro em binario*/

    printf("Este programa converte um numero inteiro decimal e binário.\n");

    int N;
    printf("Introduza o numero a converter para binário: ");
    scanf("%d", &N);



} 